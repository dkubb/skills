# Blind-reproduction validation — new-kind activation test (post-suite graduation)

Protocol: find-and-prove adjudication rule 9, run 2026-07-16 before
graduating the catalog's W3.6a activation-test entry (branch
`dkubb/add/activation-test`).

## Provenance

Harvested at symbiote-substrate rung W3.6a (59th), catalog entry "New-kind
ACTIVATION TEST" under the Delta 2026-07-15 heading. The technique is Pro's
OWN design-rubric addition (verbatim source:
`symbiote-design-rounds/pro-w36a-followup-response.md`, tail section
"DESIGN-rubric addition: constructor activation check"), prescribed after
Pro's own sketch produced a join-arm-vacuous conjunction headline
(`payloadAdapter_dispatch_exact`): for every joined node both decoder-side
conjuncts were false, so the advertised join-kind reception held as an
equivalence of two false propositions. All three inner-loop review tiers
verified sketch-FIDELITY instead of activation; the fix demoted the
conjunction to a renamed, docstringed transport corollary and moved the
join-positive reception headline to the later routed-run rung.

## Result: PASS (attempt 1), matching the Pro finding exactly

A fresh reviewer received ONLY the A8 technique text against a worktree
pinned at the pre-fix state `f9a9777` (spoiler files excluded), and:

- Built the activation matrix for both counted headlines of the join delta
  at a canonical positive join instance (`J := join2 L7 L9`).
- Found the Pro finding: headline `payloadAdapter_dispatch_exact` is
  VACUOUS-ON-NEW-KIND — `payloadDecode`'s image and `PayloadObsSpec`'s
  admitted set contain only childless `toolResult` leaves, so at every join
  node both conjunctions are false (`False ↔ False`), and every
  join-specific wiring mutant of `adapterUnwrapNode` (swap, reject,
  unary-accept) leaves the headline statement provable.
- Zero false positives: correctly cleared the other counted headline
  (`flatten?_isSome_iff_wellFormed`) and eleven supporting join theorems as
  POSITIVELY ACTIVATED — including locating where the join-kind adapter
  binding actually lives (the non-headline `adapterUnwrapNode_eq_flatten?`)
  and the seam-level positive witness the vacuous headline lacks.
- Applied the technique's negative-witness carve-out correctly: the four
  malformed-arity reject witnesses activated as advertised, not flagged.

Full run record:
`symbiote-design-rounds/blind-repro-activation-test-run.md`.

## Disposition

Graduated as A8 in `references/vacuity.md`, with the rubric.md entry, the
SKILL.md skeleton line, and the design-time sibling as item 5 in
formal-design `references/recon.md` (Pro prescribed it as a DESIGN-time
check: a sketch can carry the vacuity before any code exists, and the
catalog routes it as "a formal-design recon/design-round checklist item + a
find-and-prove A-class sibling"). A new letter rather than an A1 extension:
A1 fires once per suite at the do-nothing model; A8 fires per theorem
advertised as new-kind support, at a canonical positive value, with the
activation matrix and a new-kind wiring mutant — a distinct trigger and a
distinct deciding witness (C9 is the new-item precedent; B4b's
inside-an-item precedent applies only to a converse on the same surface).
The catalog's W3.6a entry may now be struck on merge (the symbiote agent's
lane).
