# Graduation draft — new-kind activation test (blind-repro gate UNMET)

Status: **DRAFT — do not merge until adjudication rule 9 is satisfied.**
Written 2026-07-16 on branch `dkubb/add/activation-test`.

## Provenance

Harvested at symbiote-substrate rung W3.6a (59th), catalog entry "New-kind
ACTIVATION TEST" under the Delta 2026-07-15 heading. The technique is Pro's
OWN design-rubric addition (verbatim source:
`symbiote-design-rounds/pro-w36a-followup-response.md`, tail section
"DESIGN-rubric addition: constructor activation check"), prescribed after
Pro's own sketch produced a join-arm-vacuous conjunction headline
(`payloadDecode_and_adapterUnwrap_exact`): for every joined node both
decoder-side conjuncts were false, so the advertised join-kind reception held
as an equivalence of two false propositions. All three inner-loop review
tiers verified sketch-FIDELITY instead of activation; the fix demoted the
conjunction to a docstringed transport corollary and moved the join-positive
reception headline to the later routed-run rung.

## Placement

New item **A8** in find-and-prove `references/vacuity.md` (with the rubric.md
entry and the SKILL.md skeleton line), plus a sketch-time item 5 in
formal-design `references/recon.md` — Pro prescribed it as a DESIGN-time
check, and the catalog routes it as "a formal-design recon/design-round
checklist item + a find-and-prove A-class sibling". A new letter rather than
an A1 extension: A1 fires once per suite at the do-nothing model; A8 fires
per theorem advertised as new-kind support, at a canonical positive value,
with the activation matrix and a new-kind wiring mutant — a distinct trigger
and a distinct deciding witness (C9 is the new-item precedent; B4b's
inside-an-item precedent applies only to a converse on the same surface).

## Validation basis and the unmet gate

No blind reproduction has been run. The defect was demonstrated in-tree at
W3.6a (by Pro, the technique's own author) and the fix landed — but
adjudication rule 9 is unconditional, and the C9 precedent ran its blind
repro even though Codex and Pro had already reproduced that finding in-tree.
Prior in-tree demonstration therefore does not waive the gate.

Required before merge: a fresh subagent, armed with the A8 lens text alone at
the pre-fix W3.6a state (or a filter-safe synthetic puzzle with a planted
two-false-props conjunction headline), reproduces the finding on attempt 1.
On PASS: add the `blind-reproduction-activation-test.md` report, the
reconciliation-report shed row, and strike the catalog copy (the symbiote
agent's lane). On FAIL: sharpen the lens text and re-run; do not merge.
